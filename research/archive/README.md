# `research/archive/`

Superseded material kept for history rather than deleted, per this
project's "move instead of recreate/delete" convention.

- `early_drafts/ncf_formulation_early_draft.ipynb` — an earlier, rougher
  draft that was rewritten into the polished
  `research/notebooks/01_ncf_formulation.ipynb`. Confirmed via content
  comparison in Phase 6 that the polished version is a strict superset
  (same core derivation, plus the hypothesis and evaluation protocol
  sections this draft didn't have yet) before archiving rather than
  deleting outright.
