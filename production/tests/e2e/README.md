# End-to-end tests

Full-stack tests driving `apps/web` against a real `backend` +
`model_server`. Empty scaffold added in this hardening pass — to be
implemented with Playwright (already a stray dependency in
`requirements.txt`, flagged in the Phase 1 audit for possible reuse here
instead of removal — decide in Phase 5).
