# Grafana dashboards (reference scaffolding)

Placeholder for dashboard JSON exports (request latency, model inference
time, HR@10 drift over time). Add exported dashboard JSON files under
`dashboards/` once Prometheus metrics exist to visualize (see
`deployment/monitoring/prometheus/prometheus.yml`).
