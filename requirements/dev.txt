-r base.txt

# ── Testing ─────────────────────────────────────────────────
pytest==8.2.2
pytest-asyncio==0.23.8
pytest-cov>=5.0.0

# ── Lint / type-check ──────────────────────────────────────
ruff>=0.5.0
mypy>=1.10.0
pre-commit>=3.7.0

# ── Reporting (from original requirements.txt "Reporting Fixes"
#    section — nbconvert/mistune support thesis notebook rendering;
#    playwright is flagged as possibly-stray in the Phase 1 audit,
#    kept here pending the Phase 5 decision, not deleted) ────────
nbconvert==7.16.4
mistune<3.0.0
playwright==1.42.0
